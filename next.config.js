/* module.exports = {
    webpack: config => {
        config.module.rules.push({
            test: /\.(woff2)$/,
            use: {
                loader: 'file-loader',
                options: {
                    name: '[name].[ext]',
                },
            },
        });
        return config;
    },
}; */